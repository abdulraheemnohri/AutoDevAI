# Placeholder for prompt building logic

def build_prompt(command, context):
    """Builds a detailed prompt based on the command and context."""
    # This function would dynamically construct prompts based on the AI command
    # and relevant context (e.g., code snippets, documentation, issues).
    return f"Please {command} the following: {context}"
